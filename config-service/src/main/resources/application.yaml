server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Truncus12/Tingeso2_Back
          search-paths: config-data
  application:
    name: config-service
